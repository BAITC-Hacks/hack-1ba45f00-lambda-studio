# AGENTS.md

Этот репозиторий описан в **`CLAUDE.md`** — прочитай его целиком перед первой задачей. Все правила,
контракты и пороги там; этот файл только указатель для Codex и других агентов.

С 15:40 весь бэкенд (`mycelium/`, `tests/`) ведёт участник A (`CLAUDE.md` §17). Если тебя позвали
помочь — не правь чужие файлы без согласования с A. Форма ответов API — `docs/FRONTEND.md` §4 и
`docs/mock/`. gid в JSON — только строки.
